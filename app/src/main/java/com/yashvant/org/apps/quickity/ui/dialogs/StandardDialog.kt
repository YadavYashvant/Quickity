package com.yashvant.org.apps.quickity.ui.dialogs

/*
import android.app.Dialog
import android.content.Context
import com.saadahmedev.popupdialog.PopupDialog
import com.saadahmedev.popupdialog.listener.StandardDialogActionListener
import com.yashvant.org.apps.qrscanner.R


fun StandardDialog(context: Context) {
    PopupDialog.getInstance(context)
        .standardDialogBuilder()
        .createStandardDialog()
        .setHeading("Logout")
        .setDescription(
            "Are you sure you want to logout?" +
                    " This action cannot be undone"
        )
        .setIcon(R.drawable.outline_qr_code_scanner_24)
        .setIconColor(R.color.purple_200)
        .build(object : StandardDialogActionListener {
            override fun onPositiveButtonClicked(dialog: Dialog) {
                dialog.dismiss()
            }

            override fun onNegativeButtonClicked(dialog: Dialog) {
                dialog.dismiss()
            }
        })
        .show()
}*/
